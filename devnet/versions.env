# Pinned versions of the components the devnet runs against.
#
# The chain builds from this repository; everything else it needs is a released
# artefact from another repository, pinned here rather than built from a sibling
# checkout. That is the whole point of the split: the devnet exercises the same
# artefacts an operator would run, not whatever happens to be in a neighbouring
# working tree.
#
# Both have a local-build escape hatch for cross-repo development, which is the
# one case where you *do* want the sibling checkout:
#
#   make dev-up   GUARDIAND_BIN=~/dev/timeflareio/guardian/bin/guardiand
#   make e2e      SDK_DIR=~/dev/timeflareio/typescript-sdk
#
# Bump these deliberately: a version here is a claim about what the chain is
# known to work with, and it feeds COMPATIBILITY.md.

# github.com/timeflareio/guardian — release providing the guardiand and guardianctl
# binaries. TWO binaries as of v0.0.4, which split the surface: guardiand runs the
# service, guardianctl sets a guardian up and holds every verb that can write key
# material. The devnet drives both, from the same tag — they share a configuration
# schema, so a mismatched pair means one writes a file the other cannot read.
#
# v0.0.4 is the floor for this repository: guardiand-sync fetches both assets, and
# no earlier release publishes guardianctl at all.
GUARDIAN_VERSION=v0.0.5

# github.com/timeflareio/typescript-sdk — release providing the SDK dist
# tarball that drives the e2e suites.
#
# The EXAMPLES bundle from this release drives the e2e harness. The same release
# also publishes a dist-only tarball for consumers that resolve the package by
# URL; that one has no examples/ and is not what the devnet wants.
#
# The floor is v0.0.5. From v0.0.4 the package is @timeflareio/typescript-sdk and
# takes the WASM bundle as a dependency rather than shipping it, so the examples
# bundle carries no wasm/ and the harness gets the bundle by installing the
# bundle's dependencies — which sdk-install does. v0.0.5 is the floor rather than
# v0.0.4 because the scenario suite's raw-WASM escape hatch
# (examples/lib.js loadWasmPrimitives) only resolves through that dependency from
# v0.0.5; on v0.0.4 it still looked for the absent wasm/ directory and no
# scenario could create a secret.
SDK_VERSION=v0.0.6
